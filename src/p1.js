const { nextInt } = require('../lib/fake-input');
// const { nextInt } = require('../lib/real-input');

// Write a program that reads an unspecified number of integers.
// Count how many positive and negative numbers are entered.
// Compute the average and the sum of all values.
// Print the number of positive integers entered.
// Print the number of negative integers entered.
// Print the average of all values entered (with two decimal places).
// Print the sum of all values entered (with two decimal places).
// Example input and output:
// Input: 3 -2 5 1 -3
// Displayed:
//   Positive: 3
//   Negative: 2
//   Average 0.80
//   Sum: 4.00
// Output: [3, 2, '0.80', '4.00']
function p1() {
    // TODO: Complete the program
}

module.exports = p1;
